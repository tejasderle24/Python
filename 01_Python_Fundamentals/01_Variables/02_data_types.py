"""
# Python supports several built-in data types:
    Integers (int): Whole numbers (e.g., 10, -5).
    Floats (float): Decimal numbers (e.g., 3.14, -0.001).
    Strings (str): Text data enclosed in quotes (e.g., "Hello", 'Python').
    Booleans (bool): Represents True or False.
    Lists: Ordered, mutable collections (e.g., [1, 2, 3]).
    Tuples: Ordered, immutable collections (e.g., (1, 2, 3)).
    Sets: Unordered collections of unique elements (e.g., {1, 2, 3}).
    Dictionaries: Key-value pairs (e.g., {"name": "Alice", "age": 25}).

"""
# Integers

age = 33
print(age)
print(type(age))   # Check the datatype

# Floats

cgpa = 8.6
print(cgpa)
print(type(cgpa))   # Check the datatype

# Strings

name = "Tejas"
print(name)
print(type(name))   # Check the datatype

# Booleans => value must be first letter is Capital

is_complated = True # is must be False
print(is_complated)
print(type(is_complated))   # Check the datatype
